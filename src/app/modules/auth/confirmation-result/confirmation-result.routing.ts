import { Route } from '@angular/router';
import { ConfirmationResultComponent } from './confirmation-result.component';

export const authConfirmationResultRoutes: Route[] = [
    {
        path     : '',
        component: ConfirmationResultComponent
    }
];
